

#include "HAL.h"

// static const struct pwm_dt_spec Buzz_DT = PWM_DT_SPEC_GET(DT_ALIAS(buzzer));



void HAL::Buzz_Init(void)
{
    // if (!pwm_is_ready_dt(&Buzz_DT)) {
	// 	printk("Error: PWM device %s is not ready\n",Buzz_DT.dev->name);
	// 	return;
	// }
    // pwm_set_dt(&Buzz_DT, 0, 0);
}